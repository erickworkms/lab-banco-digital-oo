package org.contas.dao;

import org.contas.dto.ContaDTO;
import org.contas.model.Cliente;

import java.util.List;

public class VerSaldo {
/*
    public boolean verSaqueUsuario(ContaDTO contaDTO, List<Cliente> clientes) {
        for (Cliente verCliente : clientes) {
            if (contaDTO.getCliente().getCpf().equals(verCliente.getCpf())) {
                return false;
            }
        }
        return true;
    }*/
}
